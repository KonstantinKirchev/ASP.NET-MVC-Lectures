@using PagedList.Mvc
@model PagedList.IPagedList<Bookmarks.App.Models.ViewModels.BookmarkViewModel>

@{
    ViewBag.Title = "All Bookmarks";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    @Html.DisplayFor(x => x)
</div>
@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))